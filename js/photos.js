/* Listing photographs from Wikimedia Commons, keyed by listing id.
   Each entry: src (path without size suffix), alt, author, license, licenseUrl, page, title. */
window.NHM = window.NHM || {};
NHM.photos = {};
